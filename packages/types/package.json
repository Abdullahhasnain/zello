{
  "name": "@zello-ai/types",
  "private": true,
  "version": "0.1.0",
  "description": "Shared TypeScript types for Zello AI frontends — hand-written domain types now; the api-v1.ts surface is meant to be regenerated from the backend's OpenAPI schema once endpoints exist (openapi-typescript).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate:api-types": "openapi-typescript http://localhost:8000/api/v1/openapi.json -o src/api-v1.ts"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.1",
    "typescript": "^5.6.3"
  }
}
